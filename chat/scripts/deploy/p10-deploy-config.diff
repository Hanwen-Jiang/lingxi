diff --git a/deploy/.env b/deploy/.env
--- a/deploy/.env
+++ b/deploy/.env
@@
 RAG_RERANK_ENDPOINT=http://127.0.0.1:18080/rerank
 AGENT_SERVICE_PORT=10011
 WEB_PORT=4173
+JWT_SECRET_KEY=lingxiProdHS512Secret_2026_aB7kQ9zR3xT1vN6mYpE4wD8cF0jL5hG2
+INTERNAL_SERVICE_TOKEN=lingxi-internal-prod-7f3a9c2e5b1d4068
+AGENT_GATEWAY_URI=http://agent:10011
+AGENT_GATEWAY_ENFORCE_IDENTITY=true
+GATEWAY_CORS_ALLOWED_ORIGIN_PATTERNS=http://localhost:[*],http://127.0.0.1:[*],http://100.*:[*]
 SUDO_PASS=asdf1476
diff --git a/deploy/docker-compose.yml b/deploy/docker-compose.yml
--- a/deploy/docker-compose.yml
+++ b/deploy/docker-compose.yml
@@
     environment: &chat_env
       JAVA_OPTS: ${JAVA_APP_OPTS:--Xms128m -Xmx512m}
+      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
+      INTERNAL_SERVICE_TOKEN: ${INTERNAL_SERVICE_TOKEN}
       MYSQL_URL: jdbc:mysql://mysql:3306/InfiniteChat?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
       MYSQL_USERNAME: ${MYSQL_USERNAME}
       MYSQL_PASSWORD: ${MYSQL_PASSWORD}
@@
     environment:
       <<: *chat_env
-      GATEWAY_CORS_ALLOWED_ORIGIN: "http://127.0.0.1:${WEB_PORT}"
+      AGENT_GATEWAY_URI: ${AGENT_GATEWAY_URI}
+      GATEWAY_CORS_ALLOWED_ORIGIN_PATTERNS: ${GATEWAY_CORS_ALLOWED_ORIGIN_PATTERNS}
     ports:
       - "10010:10010"
@@
       AGENT_MODEL_OPENAI_COMPATIBLE_MAX_OUTPUT_TOKENS: ${AGENT_MODEL_OPENAI_COMPATIBLE_MAX_OUTPUT_TOKENS}
       AGENT_MODEL_OPENAI_COMPATIBLE_TEMPERATURE: ${AGENT_MODEL_OPENAI_COMPATIBLE_TEMPERATURE}
       MCP_ENABLED: ${MCP_ENABLED}
       WEB_SEARCH_ENABLED: ${WEB_SEARCH_ENABLED}
+      AGENT_GATEWAY_ENFORCE_IDENTITY: ${AGENT_GATEWAY_ENFORCE_IDENTITY}
       RAG_RERANK_PROVIDER: ${RAG_RERANK_PROVIDER}
       RAG_RERANK_ENDPOINT: ${RAG_RERANK_ENDPOINT}
       RAG_DOCS_PATH: /app/docs
diff --git a/deploy/init-chat-schema.sql b/deploy/init-chat-schema.sql
--- a/deploy/init-chat-schema.sql
+++ b/deploy/init-chat-schema.sql
@@
-CREATE TABLE IF NOT EXISTS `message_outbox` (
-  `id` BIGINT AUTO_INCREMENT NOT NULL,
-  `message_id` BIGINT NULL DEFAULT 0,
-  `topic` VARCHAR(255) NULL,
-  `message_key` VARCHAR(255) NULL,
-  `payload` TEXT NULL,
-  `status` INT NULL DEFAULT 0,
-  `retry_count` INT NULL DEFAULT 0,
-  `next_retry_at` DATETIME NULL,
-  `last_error` VARCHAR(255) NULL,
-  `created_at` DATETIME NULL DEFAULT CURRENT_TIMESTAMP,
-  `updated_at` DATETIME NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
-  PRIMARY KEY (`id`)
-) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
-
 CREATE TABLE IF NOT EXISTS `moment` (
@@
-  `status` INT NULL DEFAULT 0,
+  `status` INT NULL DEFAULT 1,
   `created_at` DATETIME NULL DEFAULT CURRENT_TIMESTAMP,
   `updated_at` DATETIME NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
@@
   `session_id` BIGINT NULL DEFAULT 0,
   `role` INT NULL DEFAULT 0,
   `status` INT NULL DEFAULT 0,
+  `last_read_message_id` BIGINT NULL DEFAULT NULL,
   `created_at` DATETIME NULL DEFAULT CURRENT_TIMESTAMP,
   `updated_at` DATETIME NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
@@
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
+
+ALTER TABLE `user` MODIFY COLUMN `status` INT NULL DEFAULT 1;
+UPDATE `user` SET `status` = 1 WHERE `status` = 0;
+ALTER TABLE `user_session` ADD COLUMN IF NOT EXISTS `last_read_message_id` BIGINT NULL DEFAULT NULL;
 
 ALTER TABLE `red_packet` ADD COLUMN IF NOT EXISTS `expire_at` DATETIME NULL;
